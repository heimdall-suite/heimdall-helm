#include "board.h"

#error \
    "nexus_xr board.c is a structural placeholder, not a real port: no " \
    "confirmed HSE crystal value and no bench-verified pin map exist yet " \
    "(see board.h and aoa-boat-controller's docs/nexus-xr-exploration.md). " \
    "Fill this in against real hardware or a real schematic, not guesses, " \
    "then delete this #error."

void board_init(void) {
}
